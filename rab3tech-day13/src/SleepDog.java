public class SleepDog extends BarkDog{
	String color="white";
    //u can access ((tail)BarkDog class + (leg + name)Dog class)
	
	// we can access (bark()BarkDog class + (run()+walk()Dog class))
	public void sleep() {
		System.out.println("Dog can sleep");
	}
}

